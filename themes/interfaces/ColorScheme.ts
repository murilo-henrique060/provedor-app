/**
 * ColorScheme
 * 
 * This is used to define the color scheme of the app.
 * It contains the colors that are used in the app.
 */
interface ColorScheme {
  // Main Colors of the App
  primary: string;
  secondary: string;
  body: string;

  // Specific Colors
  whatsapp: string;
  
  // Text Colors
  textPrimary: string;
  textBody: string;
}

export default ColorScheme;